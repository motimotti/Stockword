# Stockword 要件定義書

## 1. プロジェクト概要

**プロジェクト名**: Stockword  
**目的**: IT・ビジネス用語を入力すると、Claude AIが読み方・語源・意味・穴埋め問題を自動生成する学習支援ツール  
**対象ユーザー**: IT用語・ビジネス用語を学習したい日本語話者

---

## 2. 機能要件

### 2.1 用語検索
- テキスト入力欄に用語を入力し、「検索」ボタンまたはEnterキーで検索できる
- 入力は1〜100文字の範囲を受け付ける
- 空文字・空白のみの入力はエラーメッセージを表示する

### 2.2 用語カード表示
検索結果として以下の情報を含むカードを表示する：

| 項目 | 内容 | 例 |
|------|------|-----|
| 用語名 | 入力された用語 | API |
| 読み方 | ひらがな・カタカナ表記 | えーぴーあい |
| 英語正式名称 | フルスペル | Application Programming Interface |
| 語源 | 構成要素の分解説明（任意） | Application（アプリ）+ ... |
| 意味 | 50文字以内の日本語説明 | 異なるソフトウェア同士が... |
| 穴埋め① | 1箇所空欄の問題と答え | APIとは、（　　　）である。 |
| 穴埋め② | 2箇所空欄の問題と答え | （①）同士が…（②）。 |

### 2.3 穴埋め問題仕様
- **穴埋め①**: `（　　　）` 形式、1箇所のみ、用語の本質を問う
- **穴埋め②**: `（①）`・`（②）` 形式、2箇所、①②は異なる概念を問う
- ①②の答えはカード内に表示する（学習・確認用途）

---

## 3. 非機能要件

### 3.1 技術スタック
| 層 | 技術 |
|----|------|
| フロントエンド | Streamlit（Python） |
| AI生成 | Claude API（claude-sonnet-4-6） |
| 言語 | Python 3.x |

### 3.2 パフォーマンス
- プロンプトキャッシュ（`cache_control: ephemeral`）によりシステムプロンプトを再利用し、2回目以降の応答コスト・速度を改善する
- 同一用語の検索結果はセッション中にキャッシュする（`@st.cache_data`）

### 3.3 セキュリティ
- APIキーは `.env` ファイルで管理し、Gitにコミットしない
- 入力値は100文字上限でバリデーションする

### 3.4 エラー処理
| エラー種別 | 対応 |
|-----------|------|
| 空入力 | 「用語を入力してください」を表示 |
| APIキー未設定 | 「ANTHROPIC_API_KEY が設定されていません」を表示 |
| API通信エラー | 「情報の取得に失敗しました」を表示 |
| JSONパース失敗 | 正規表現でJSON部分を抽出して再試行 |

---

## 4. Claude APIプロンプト仕様

### 4.1 出力JSON構造
```json
{
  "term": "string",
  "reading": "string",
  "english": "string",
  "etymology": "string（空文字可）",
  "meaning": "string（50文字以内）",
  "fillBlank1": { "question": "string", "answer": "string" },
  "fillBlank2": { "question": "string", "answers": ["string", "string"] }
}
```

### 4.2 プロンプト設計方針
- システムプロンプトにJSONスキーマ・ルール・ジャンル例を記載
- マークダウン出力を禁止し、JSONのみを返すよう明示
- 語源がない用語は `etymology` を空文字とする
- `answers` は必ず2要素の配列とする

---

## 5. 起動方法

```bash
# 依存パッケージインストール
pip install -r requirements.txt

# APIキー設定（.envファイル）
ANTHROPIC_API_KEY=sk-ant-xxxxxx

# 起動
streamlit run app.py
# → http://localhost:8501 をブラウザで開く
```

---

## 6. ファイル構成

```
Stockword/
├── app.py              # Streamlitアプリ本体
├── requirements.txt    # Python依存パッケージ
├── .env                # APIキー（Git管理外）
├── .gitignore
└── src/                # Next.js版（参考実装）
    ├── app/
    ├── components/
    ├── hooks/
    └── lib/
```

---

## 7. 対応用語ジャンル

- ネットワーク・インターネット技術（API、DNS、VPN 等）
- ソフトウェア開発（アジャイル、CI/CD、Docker 等）
- データ・AI（機械学習、ディープラーニング 等）
- ビジネス（KPI、ROI、PDCAサイクル 等）
- セキュリティ（暗号化、フィッシング、OAuth 等）
- クラウド・インフラ（IaaS、SaaS、オートスケーリング 等）
